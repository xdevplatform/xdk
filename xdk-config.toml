# XDK Configuration
# This file contains configuration for all SDK generation

[versions]
# Python SDK version
python = "0.8.1"

# TypeScript SDK version
typescript = "0.4.0"

# Elixir SDK version
elixir = "1.0.0"
