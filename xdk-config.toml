# XDK Configuration
# This file contains configuration for all SDK generation

[versions]
# Python SDK version
python = "0.9.0"

# TypeScript SDK version
typescript = "0.5.0"

# Ruby SDK version
ruby = "0.1.0"
