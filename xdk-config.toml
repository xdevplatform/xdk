# XDK Configuration
# This file contains configuration for all SDK generation

[versions]
# Python SDK version
python = "0.9.0"

# TypeScript SDK version
typescript = "0.5.0"

# Elixir SDK version
elixir = "1.0.0"
