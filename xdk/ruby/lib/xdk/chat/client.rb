# AUTO-GENERATED FILE - DO NOT EDIT
# This file was automatically generated by the XDK build tool.
# Any manual changes will be overwritten on the next generation.
# frozen_string_literal: true

# Auto-generated chat client for the X API.
#
# This module provides a client for interacting with the chat
# endpoints of the X API. All methods, parameters, and response handling are
# generated from the OpenAPI specification.
#
# Generated automatically - do not edit manually.

require 'json'
require 'net/http'
require 'uri'
require_relative 'models'

module Xdk
  # Client for chat operations
  class ChatClient
    # @param client [Xdk::Client] the main API client
    def initialize(client)
      @client = client
    end

    # Get Chat Conversations

    # Retrieves a list of Chat conversations for the authenticated user's inbox.

    # @param max_results [Integer, nil] Maximum number of conversations to return.

    # @param pagination_token [String, nil] Token for pagination to retrieve the next page of results.

    # @param chat_conversation_fields [Array, nil] A comma separated list of ChatConversation fields to display.

    # @param expansions [Array, nil] A comma separated list of fields to expand.

    # @param user_fields [Array, nil] A comma separated list of User fields to display.

    # @return [Hash] API response
    def get_conversations(max_results: nil, pagination_token: nil, chat_conversation_fields: nil, expansions: nil,
                          user_fields: nil)
      # Build the request path
      path = '/2/chat/conversations'

      # Build query parameters
      params = {}

      params['max_results'] = max_results unless max_results.nil?

      params['pagination_token'] = pagination_token unless pagination_token.nil?

      unless chat_conversation_fields.nil?
        params['chat_conversation.fields'] =
          chat_conversation_fields.is_a?(Array) ? chat_conversation_fields.join(',') : chat_conversation_fields
      end

      params['expansions'] = expansions.is_a?(Array) ? expansions.join(',') : expansions unless expansions.nil?

      params['user.fields'] = user_fields.is_a?(Array) ? user_fields.join(',') : user_fields unless user_fields.nil?

      @client.request(method: :get, path: path, params: params)
    end

    # Send Typing Indicator

    # Sends a typing indicator to a specific Chat conversation on behalf of the authenticated user.

    # @param conversation_id [Object] The Chat conversation ID. (required)

    # @return [Hash] API response
    def send_typing_indicator(conversation_id:)
      # Build the request path
      path = '/2/chat/conversations/{conversation_id}/typing'

      path = path.gsub('{conversation_id}', conversation_id.to_s)

      # Build query parameters
      params = {}

      @client.request(method: :post, path: path, params: params)
    end

    # Download Chat Media

    # Downloads encrypted media bytes from an XChat conversation. The response body contains raw binary bytes with content type application/octet-stream, despite the application/json declaration here (a platform limitation).

    # @param conversation_id [Object] The Chat conversation ID. (required)

    # @param media_hash_key [Object] The media hash key returned from the upload initialize step. (required)

    # @return [Hash] API response
    def media_download(conversation_id:, media_hash_key:)
      # Build the request path
      path = '/2/chat/media/{conversation_id}/{media_hash_key}'

      path = path.gsub('{conversation_id}', conversation_id.to_s)

      path = path.gsub('{media_hash_key}', media_hash_key.to_s)

      # Build query parameters
      params = {}

      @client.request(method: :get, path: path, params: params)
    end

    # Get user public keys

    # Returns the public keys and Juicebox configuration for the specified user.

    # @param id [Object] The ID of the User to lookup. (required)

    # @param public_key_fields [Array, nil] A comma separated list of PublicKey fields to display.

    # @return [Hash] API response
    def get_user_public_keys(id:, public_key_fields: nil)
      # Build the request path
      path = '/2/users/{id}/public_keys'

      path = path.gsub('{id}', id.to_s)

      # Build query parameters
      params = {}

      unless public_key_fields.nil?
        params['public_key.fields'] =
          public_key_fields.is_a?(Array) ? public_key_fields.join(',') : public_key_fields
      end

      @client.request(method: :get, path: path, params: params)
    end

    # Add public key

    # Registers a user's public key for X Chat encryption.

    # @param id [Object] The ID of the requesting user. (required)

    # @param body [Hash] Request body

    # @return [Hash] API response
    def add_user_public_key(id:, body:)
      # Build the request path
      path = '/2/users/{id}/public_keys'

      path = path.gsub('{id}', id.to_s)

      # Build query parameters
      params = {}

      # Prepare request body
      if body.is_a?(Hash)
        body
      else
        (body.nil? ? nil : body)
      end

      @client.request(method: :post, path: path, params: params)
    end

    # Finalize Chat Media Upload

    # Finalizes an XChat media upload session.

    # @param id [String] The session/resume id from initialize. (required)

    # @param body [Hash] Request body

    # @return [Hash] API response
    def media_upload_finalize(id:, body:)
      # Build the request path
      path = '/2/chat/media/upload/{id}/finalize'

      path = path.gsub('{id}', id.to_s)

      # Build query parameters
      params = {}

      # Prepare request body
      if body.is_a?(Hash)
        body
      else
        (body.nil? ? nil : body)
      end

      @client.request(method: :post, path: path, params: params)
    end

    # Initialize Chat Media Upload

    # Initializes an XChat media upload session.

    # @param body [Hash] Request body

    # @return [Hash] API response
    def media_upload_initialize(body:)
      # Build the request path
      path = '/2/chat/media/upload/initialize'

      # Build query parameters
      params = {}

      # Prepare request body
      if body.is_a?(Hash)
        body
      else
        (body.nil? ? nil : body)
      end

      @client.request(method: :post, path: path, params: params)
    end

    # Mark Conversation as Read

    # Marks a specific Chat conversation as read on behalf of the authenticated user.

    # @param conversation_id [Object] The Chat conversation ID. (required)

    # @param body [Hash] Request body

    # @return [Hash] API response
    def mark_conversation_read(conversation_id:, body:)
      # Build the request path
      path = '/2/chat/conversations/{conversation_id}/read'

      path = path.gsub('{conversation_id}', conversation_id.to_s)

      # Build query parameters
      params = {}

      # Prepare request body
      if body.is_a?(Hash)
        body
      else
        (body.nil? ? nil : body)
      end

      @client.request(method: :post, path: path, params: params)
    end

    # Append Chat Media Upload

    # Appends media data to an XChat upload session.

    # @param id [String] The session/resume id from initialize. (required)

    # @param body [Hash] Request body

    # @return [Hash] API response
    def media_upload_append(id:, body:)
      # Build the request path
      path = '/2/chat/media/upload/{id}/append'

      path = path.gsub('{id}', id.to_s)

      # Build query parameters
      params = {}

      # Prepare request body
      if body.is_a?(Hash)
        body
      else
        (body.nil? ? nil : body)
      end

      @client.request(method: :post, path: path, params: params)
    end

    # Send Chat Message

    # Sends an encrypted message to a specific Chat conversation.

    # @param conversation_id [Object] The Chat conversation ID. (required)

    # @param body [Hash] Request body

    # @return [Hash] API response
    def send_message(conversation_id:, body:)
      # Build the request path
      path = '/2/chat/conversations/{conversation_id}/messages'

      path = path.gsub('{conversation_id}', conversation_id.to_s)

      # Build query parameters
      params = {}

      # Prepare request body
      if body.is_a?(Hash)
        body
      else
        (body.nil? ? nil : body)
      end

      @client.request(method: :post, path: path, params: params)
    end

    # Get Chat Conversation

    # Retrieves messages and key change events for a specific Chat conversation with pagination support.

    # @param conversation_id [Object] The Chat conversation ID. (required)

    # @param max_results [Integer, nil] Maximum number of message events to return.

    # @param pagination_token [String, nil] Token for pagination to retrieve the next page of results.

    # @param chat_message_event_fields [Array, nil] A comma separated list of ChatMessageEvent fields to display.

    # @return [Hash] API response
    def get_conversation(conversation_id:, max_results: nil, pagination_token: nil, chat_message_event_fields: nil)
      # Build the request path
      path = '/2/chat/conversations/{conversation_id}'

      path = path.gsub('{conversation_id}', conversation_id.to_s)

      # Build query parameters
      params = {}

      params['max_results'] = max_results unless max_results.nil?

      params['pagination_token'] = pagination_token unless pagination_token.nil?

      unless chat_message_event_fields.nil?
        params['chat_message_event.fields'] =
          chat_message_event_fields.is_a?(Array) ? chat_message_event_fields.join(',') : chat_message_event_fields
      end

      @client.request(method: :get, path: path, params: params)
    end
  end
end
