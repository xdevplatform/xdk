# AUTO-GENERATED FILE - DO NOT EDIT
# This file was automatically generated by the XDK build tool.
# Any manual changes will be overwritten on the next generation.
# frozen_string_literal: true

# Auto-generated lists client for the X API.
#
# This module provides a client for interacting with the lists
# endpoints of the X API. All methods, parameters, and response handling are
# generated from the OpenAPI specification.
#
# Generated automatically - do not edit manually.

require 'json'
require 'net/http'
require 'uri'
require_relative 'models'

module Xdk
  # Client for lists operations
  class ListsClient
    # @param client [Xdk::Client] the main API client
    def initialize(client)
      @client = client
    end

    # Remove List member

    # Removes a User from a specific List by its ID and the User’s ID.

    # @param id [Object] The ID of the List to remove a member. (required)

    # @param user_id [Object] The ID of User that will be removed from the List. (required)

    # @return [Hash] API response
    def remove_member_by_user_id(id:, user_id:)
      # Build the request path
      path = '/2/lists/{id}/members/{user_id}'

      path = path.gsub('{id}', id.to_s)

      path = path.gsub('{user_id}', user_id.to_s)

      # Build query parameters
      params = {}

      @client.request(method: :delete, path: path, params: params)
    end

    # Get List members

    # Retrieves a list of Users who are members of a specific List by its ID.

    # @param id [Object] The ID of the List. (required)

    # @param max_results [Integer, nil] The maximum number of results.

    # @param pagination_token [Object, nil] This parameter is used to get a specified 'page' of results.

    # @param user_fields [Array, nil] A comma separated list of User fields to display.

    # @param expansions [Array, nil] A comma separated list of fields to expand.

    # @param tweet_fields [Array, nil] A comma separated list of Tweet fields to display.

    # @return [Hash] API response
    def get_members(id:, max_results: nil, pagination_token: nil, user_fields: nil, expansions: nil, tweet_fields: nil)
      # Build the request path
      path = '/2/lists/{id}/members'

      path = path.gsub('{id}', id.to_s)

      # Build query parameters
      params = {}

      params['max_results'] = max_results unless max_results.nil?

      params['pagination_token'] = pagination_token unless pagination_token.nil?

      params['user.fields'] = user_fields.is_a?(Array) ? user_fields.join(',') : user_fields unless user_fields.nil?

      params['expansions'] = expansions.is_a?(Array) ? expansions.join(',') : expansions unless expansions.nil?

      unless tweet_fields.nil?
        params['tweet.fields'] =
          tweet_fields.is_a?(Array) ? tweet_fields.join(',') : tweet_fields
      end

      @client.request(method: :get, path: path, params: params)
    end

    # Add List member

    # Adds a User to a specific List by its ID.

    # @param id [Object] The ID of the List for which to add a member. (required)

    # @param body [Hash] Request body

    # @return [Hash] API response
    def add_member(id:, body: nil)
      # Build the request path
      path = '/2/lists/{id}/members'

      path = path.gsub('{id}', id.to_s)

      # Build query parameters
      params = {}

      # Prepare request body
      if body.is_a?(Hash)
        body
      else
        (body.nil? ? nil : body)
      end

      @client.request(method: :post, path: path, params: params)
    end

    # Get List by ID

    # Retrieves details of a specific List by its ID.

    # @param id [Object] The ID of the List. (required)

    # @param list_fields [Array, nil] A comma separated list of List fields to display.

    # @param expansions [Array, nil] A comma separated list of fields to expand.

    # @param user_fields [Array, nil] A comma separated list of User fields to display.

    # @return [Hash] API response
    def get_by_id(id:, list_fields: nil, expansions: nil, user_fields: nil)
      # Build the request path
      path = '/2/lists/{id}'

      path = path.gsub('{id}', id.to_s)

      # Build query parameters
      params = {}

      params['list.fields'] = list_fields.is_a?(Array) ? list_fields.join(',') : list_fields unless list_fields.nil?

      params['expansions'] = expansions.is_a?(Array) ? expansions.join(',') : expansions unless expansions.nil?

      params['user.fields'] = user_fields.is_a?(Array) ? user_fields.join(',') : user_fields unless user_fields.nil?

      @client.request(method: :get, path: path, params: params)
    end

    # Update List

    # Updates the details of a specific List owned by the authenticated user by its ID.

    # @param id [Object] The ID of the List to modify. (required)

    # @param body [Hash] Request body

    # @return [Hash] API response
    def update(id:, body: nil)
      # Build the request path
      path = '/2/lists/{id}'

      path = path.gsub('{id}', id.to_s)

      # Build query parameters
      params = {}

      # Prepare request body
      if body.is_a?(Hash)
        body
      else
        (body.nil? ? nil : body)
      end

      @client.request(method: :put, path: path, params: params)
    end

    # Delete List

    # Deletes a specific List owned by the authenticated user by its ID.

    # @param id [Object] The ID of the List to delete. (required)

    # @return [Hash] API response
    def delete(id:)
      # Build the request path
      path = '/2/lists/{id}'

      path = path.gsub('{id}', id.to_s)

      # Build query parameters
      params = {}

      @client.request(method: :delete, path: path, params: params)
    end

    # Get List followers

    # Retrieves a list of Users who follow a specific List by its ID.

    # @param id [Object] The ID of the List. (required)

    # @param max_results [Integer, nil] The maximum number of results.

    # @param pagination_token [Object, nil] This parameter is used to get a specified 'page' of results.

    # @param user_fields [Array, nil] A comma separated list of User fields to display.

    # @param expansions [Array, nil] A comma separated list of fields to expand.

    # @param tweet_fields [Array, nil] A comma separated list of Tweet fields to display.

    # @return [Hash] API response
    def get_followers(id:, max_results: nil, pagination_token: nil, user_fields: nil, expansions: nil,
                      tweet_fields: nil)
      # Build the request path
      path = '/2/lists/{id}/followers'

      path = path.gsub('{id}', id.to_s)

      # Build query parameters
      params = {}

      params['max_results'] = max_results unless max_results.nil?

      params['pagination_token'] = pagination_token unless pagination_token.nil?

      params['user.fields'] = user_fields.is_a?(Array) ? user_fields.join(',') : user_fields unless user_fields.nil?

      params['expansions'] = expansions.is_a?(Array) ? expansions.join(',') : expansions unless expansions.nil?

      unless tweet_fields.nil?
        params['tweet.fields'] =
          tweet_fields.is_a?(Array) ? tweet_fields.join(',') : tweet_fields
      end

      @client.request(method: :get, path: path, params: params)
    end

    # Get List Posts

    # Retrieves a list of Posts associated with a specific List by its ID.

    # @param id [Object] The ID of the List. (required)

    # @param max_results [Integer, nil] The maximum number of results.

    # @param pagination_token [Object, nil] This parameter is used to get the next 'page' of results.

    # @param tweet_fields [Array, nil] A comma separated list of Tweet fields to display.

    # @param expansions [Array, nil] A comma separated list of fields to expand.

    # @param media_fields [Array, nil] A comma separated list of Media fields to display.

    # @param poll_fields [Array, nil] A comma separated list of Poll fields to display.

    # @param user_fields [Array, nil] A comma separated list of User fields to display.

    # @param place_fields [Array, nil] A comma separated list of Place fields to display.

    # @return [Hash] API response
    def get_posts(id:, max_results: nil, pagination_token: nil, tweet_fields: nil, expansions: nil, media_fields: nil,
                  poll_fields: nil, user_fields: nil, place_fields: nil)
      # Build the request path
      path = '/2/lists/{id}/tweets'

      path = path.gsub('{id}', id.to_s)

      # Build query parameters
      params = {}

      params['max_results'] = max_results unless max_results.nil?

      params['pagination_token'] = pagination_token unless pagination_token.nil?

      unless tweet_fields.nil?
        params['tweet.fields'] =
          tweet_fields.is_a?(Array) ? tweet_fields.join(',') : tweet_fields
      end

      params['expansions'] = expansions.is_a?(Array) ? expansions.join(',') : expansions unless expansions.nil?

      unless media_fields.nil?
        params['media.fields'] =
          media_fields.is_a?(Array) ? media_fields.join(',') : media_fields
      end

      params['poll.fields'] = poll_fields.is_a?(Array) ? poll_fields.join(',') : poll_fields unless poll_fields.nil?

      params['user.fields'] = user_fields.is_a?(Array) ? user_fields.join(',') : user_fields unless user_fields.nil?

      unless place_fields.nil?
        params['place.fields'] =
          place_fields.is_a?(Array) ? place_fields.join(',') : place_fields
      end

      @client.request(method: :get, path: path, params: params)
    end

    # Create List

    # Creates a new List for the authenticated user.

    # @param body [Hash] Request body

    # @return [Hash] API response
    def create(body: nil)
      # Build the request path
      path = '/2/lists'

      # Build query parameters
      params = {}

      # Prepare request body
      if body.is_a?(Hash)
        body
      else
        (body.nil? ? nil : body)
      end

      @client.request(method: :post, path: path, params: params)
    end
  end
end
