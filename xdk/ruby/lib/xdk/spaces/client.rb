# AUTO-GENERATED FILE - DO NOT EDIT
# This file was automatically generated by the XDK build tool.
# Any manual changes will be overwritten on the next generation.
# frozen_string_literal: true

# Auto-generated spaces client for the X API.
#
# This module provides a client for interacting with the spaces
# endpoints of the X API. All methods, parameters, and response handling are
# generated from the OpenAPI specification.
#
# Generated automatically - do not edit manually.

require 'json'
require 'net/http'
require 'uri'
require_relative 'models'

module Xdk
  # Client for spaces operations
  class SpacesClient
    # @param client [Xdk::Client] the main API client
    def initialize(client)
      @client = client
    end

    # Get Space Posts

    # Retrieves a list of Posts shared in a specific Space by its ID.

    # @param id [String] The ID of the Space to be retrieved. (required)

    # @param max_results [Integer, nil] The number of Posts to fetch from the provided space. If not provided, the value will default to the maximum of 100.

    # @param tweet_fields [Array, nil] A comma separated list of Tweet fields to display.

    # @param expansions [Array, nil] A comma separated list of fields to expand.

    # @param media_fields [Array, nil] A comma separated list of Media fields to display.

    # @param poll_fields [Array, nil] A comma separated list of Poll fields to display.

    # @param user_fields [Array, nil] A comma separated list of User fields to display.

    # @param place_fields [Array, nil] A comma separated list of Place fields to display.

    # @return [Hash] API response
    def get_posts(id:, max_results: nil, tweet_fields: nil, expansions: nil, media_fields: nil, poll_fields: nil,
                  user_fields: nil, place_fields: nil)
      # Build the request path
      path = '/2/spaces/{id}/tweets'

      path = path.gsub('{id}', id.to_s)

      # Build query parameters
      params = {}

      params['max_results'] = max_results unless max_results.nil?

      unless tweet_fields.nil?
        params['tweet.fields'] =
          tweet_fields.is_a?(Array) ? tweet_fields.join(',') : tweet_fields
      end

      params['expansions'] = expansions.is_a?(Array) ? expansions.join(',') : expansions unless expansions.nil?

      unless media_fields.nil?
        params['media.fields'] =
          media_fields.is_a?(Array) ? media_fields.join(',') : media_fields
      end

      params['poll.fields'] = poll_fields.is_a?(Array) ? poll_fields.join(',') : poll_fields unless poll_fields.nil?

      params['user.fields'] = user_fields.is_a?(Array) ? user_fields.join(',') : user_fields unless user_fields.nil?

      unless place_fields.nil?
        params['place.fields'] =
          place_fields.is_a?(Array) ? place_fields.join(',') : place_fields
      end

      @client.request(method: :get, path: path, params: params)
    end

    # Search Spaces

    # Retrieves a list of Spaces matching the specified search query.

    # @param query [String] The search query. (required)

    # @param state [String, nil] The state of Spaces to search for.

    # @param max_results [Integer, nil] The number of results to return.

    # @param space_fields [Array, nil] A comma separated list of Space fields to display.

    # @param expansions [Array, nil] A comma separated list of fields to expand.

    # @param user_fields [Array, nil] A comma separated list of User fields to display.

    # @param topic_fields [Array, nil] A comma separated list of Topic fields to display.

    # @return [Hash] API response
    def search(query:, state: nil, max_results: nil, space_fields: nil, expansions: nil, user_fields: nil,
               topic_fields: nil)
      # Build the request path
      path = '/2/spaces/search'

      # Build query parameters
      params = {}

      params['query'] = query unless query.nil?

      params['state'] = state unless state.nil?

      params['max_results'] = max_results unless max_results.nil?

      unless space_fields.nil?
        params['space.fields'] =
          space_fields.is_a?(Array) ? space_fields.join(',') : space_fields
      end

      params['expansions'] = expansions.is_a?(Array) ? expansions.join(',') : expansions unless expansions.nil?

      params['user.fields'] = user_fields.is_a?(Array) ? user_fields.join(',') : user_fields unless user_fields.nil?

      unless topic_fields.nil?
        params['topic.fields'] =
          topic_fields.is_a?(Array) ? topic_fields.join(',') : topic_fields
      end

      @client.request(method: :get, path: path, params: params)
    end

    # Get Spaces by IDs

    # Retrieves details of multiple Spaces by their IDs.

    # @param ids [Array] The list of Space IDs to return. (required)

    # @param space_fields [Array, nil] A comma separated list of Space fields to display.

    # @param expansions [Array, nil] A comma separated list of fields to expand.

    # @param user_fields [Array, nil] A comma separated list of User fields to display.

    # @param topic_fields [Array, nil] A comma separated list of Topic fields to display.

    # @return [Hash] API response
    def get_by_ids(ids:, space_fields: nil, expansions: nil, user_fields: nil, topic_fields: nil)
      # Build the request path
      path = '/2/spaces'

      # Build query parameters
      params = {}

      params['ids'] = ids.is_a?(Array) ? ids.join(',') : ids unless ids.nil?

      unless space_fields.nil?
        params['space.fields'] =
          space_fields.is_a?(Array) ? space_fields.join(',') : space_fields
      end

      params['expansions'] = expansions.is_a?(Array) ? expansions.join(',') : expansions unless expansions.nil?

      params['user.fields'] = user_fields.is_a?(Array) ? user_fields.join(',') : user_fields unless user_fields.nil?

      unless topic_fields.nil?
        params['topic.fields'] =
          topic_fields.is_a?(Array) ? topic_fields.join(',') : topic_fields
      end

      @client.request(method: :get, path: path, params: params)
    end

    # Get Spaces by creator IDs

    # Retrieves details of Spaces created by specified User IDs.

    # @param user_ids [Array] The IDs of Users to search through. (required)

    # @param space_fields [Array, nil] A comma separated list of Space fields to display.

    # @param expansions [Array, nil] A comma separated list of fields to expand.

    # @param user_fields [Array, nil] A comma separated list of User fields to display.

    # @param topic_fields [Array, nil] A comma separated list of Topic fields to display.

    # @return [Hash] API response
    def get_by_creator_ids(user_ids:, space_fields: nil, expansions: nil, user_fields: nil, topic_fields: nil)
      # Build the request path
      path = '/2/spaces/by/creator_ids'

      # Build query parameters
      params = {}

      params['user_ids'] = user_ids.is_a?(Array) ? user_ids.join(',') : user_ids unless user_ids.nil?

      unless space_fields.nil?
        params['space.fields'] =
          space_fields.is_a?(Array) ? space_fields.join(',') : space_fields
      end

      params['expansions'] = expansions.is_a?(Array) ? expansions.join(',') : expansions unless expansions.nil?

      params['user.fields'] = user_fields.is_a?(Array) ? user_fields.join(',') : user_fields unless user_fields.nil?

      unless topic_fields.nil?
        params['topic.fields'] =
          topic_fields.is_a?(Array) ? topic_fields.join(',') : topic_fields
      end

      @client.request(method: :get, path: path, params: params)
    end

    # Get space by ID

    # Retrieves details of a specific space by its ID.

    # @param id [String] The ID of the Space to be retrieved. (required)

    # @param space_fields [Array, nil] A comma separated list of Space fields to display.

    # @param expansions [Array, nil] A comma separated list of fields to expand.

    # @param user_fields [Array, nil] A comma separated list of User fields to display.

    # @param topic_fields [Array, nil] A comma separated list of Topic fields to display.

    # @return [Hash] API response
    def get_by_id(id:, space_fields: nil, expansions: nil, user_fields: nil, topic_fields: nil)
      # Build the request path
      path = '/2/spaces/{id}'

      path = path.gsub('{id}', id.to_s)

      # Build query parameters
      params = {}

      unless space_fields.nil?
        params['space.fields'] =
          space_fields.is_a?(Array) ? space_fields.join(',') : space_fields
      end

      params['expansions'] = expansions.is_a?(Array) ? expansions.join(',') : expansions unless expansions.nil?

      params['user.fields'] = user_fields.is_a?(Array) ? user_fields.join(',') : user_fields unless user_fields.nil?

      unless topic_fields.nil?
        params['topic.fields'] =
          topic_fields.is_a?(Array) ? topic_fields.join(',') : topic_fields
      end

      @client.request(method: :get, path: path, params: params)
    end

    # Get Space ticket buyers

    # Retrieves a list of Users who purchased tickets to a specific Space by its ID.

    # @param id [String] The ID of the Space to be retrieved. (required)

    # @param pagination_token [Object, nil] This parameter is used to get a specified 'page' of results.

    # @param max_results [Integer, nil] The maximum number of results.

    # @param user_fields [Array, nil] A comma separated list of User fields to display.

    # @param expansions [Array, nil] A comma separated list of fields to expand.

    # @param tweet_fields [Array, nil] A comma separated list of Tweet fields to display.

    # @return [Hash] API response
    def get_buyers(id:, pagination_token: nil, max_results: nil, user_fields: nil, expansions: nil, tweet_fields: nil)
      # Build the request path
      path = '/2/spaces/{id}/buyers'

      path = path.gsub('{id}', id.to_s)

      # Build query parameters
      params = {}

      params['pagination_token'] = pagination_token unless pagination_token.nil?

      params['max_results'] = max_results unless max_results.nil?

      params['user.fields'] = user_fields.is_a?(Array) ? user_fields.join(',') : user_fields unless user_fields.nil?

      params['expansions'] = expansions.is_a?(Array) ? expansions.join(',') : expansions unless expansions.nil?

      unless tweet_fields.nil?
        params['tweet.fields'] =
          tweet_fields.is_a?(Array) ? tweet_fields.join(',') : tweet_fields
      end

      @client.request(method: :get, path: path, params: params)
    end
  end
end
