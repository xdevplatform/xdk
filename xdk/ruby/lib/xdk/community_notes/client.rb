# AUTO-GENERATED FILE - DO NOT EDIT
# This file was automatically generated by the XDK build tool.
# Any manual changes will be overwritten on the next generation.
# frozen_string_literal: true

# Auto-generated community notes client for the X API.
#
# This module provides a client for interacting with the community notes
# endpoints of the X API. All methods, parameters, and response handling are
# generated from the OpenAPI specification.
#
# Generated automatically - do not edit manually.

require 'json'
require 'net/http'
require 'uri'
require_relative 'models'

module Xdk
  # Client for community notes operations
  class CommunityNotesClient
    # @param client [Xdk::Client] the main API client
    def initialize(client)
      @client = client
    end

    # Search for Posts Eligible for Community Notes

    # Returns all the posts that are eligible for community notes.

    # @param test_mode [Boolean] If true, return a list of posts that are for the test. If false, return a list of posts that the bots can write proposed notes on the product. (required)

    # @param pagination_token [String, nil] Pagination token to get next set of posts eligible for notes.

    # @param max_results [Integer, nil] Max results to return.

    # @param post_selection [String, nil] The selection of posts to return. Valid values are 'feed_size: [small|large|xl], feed_lang: [en|es|...|all]'. Default (if not specified) is 'feed_size: small, feed_lang: en'. Only top AI writers have access to large and xl size feeds.

    # @param tweet_fields [Array, nil] A comma separated list of Tweet fields to display.

    # @param expansions [Array, nil] A comma separated list of fields to expand.

    # @param media_fields [Array, nil] A comma separated list of Media fields to display.

    # @param poll_fields [Array, nil] A comma separated list of Poll fields to display.

    # @param user_fields [Array, nil] A comma separated list of User fields to display.

    # @param place_fields [Array, nil] A comma separated list of Place fields to display.

    # @return [Hash] API response
    def search_eligible_posts(test_mode:, pagination_token: nil, max_results: nil, post_selection: nil, tweet_fields: nil,
                              expansions: nil, media_fields: nil, poll_fields: nil, user_fields: nil, place_fields: nil)
      # Build the request path
      path = '/2/notes/search/posts_eligible_for_notes'

      # Build query parameters
      params = {}

      params['test_mode'] = test_mode unless test_mode.nil?

      params['pagination_token'] = pagination_token unless pagination_token.nil?

      params['max_results'] = max_results unless max_results.nil?

      params['post_selection'] = post_selection unless post_selection.nil?

      unless tweet_fields.nil?
        params['tweet.fields'] =
          tweet_fields.is_a?(Array) ? tweet_fields.join(',') : tweet_fields
      end

      params['expansions'] = expansions.is_a?(Array) ? expansions.join(',') : expansions unless expansions.nil?

      unless media_fields.nil?
        params['media.fields'] =
          media_fields.is_a?(Array) ? media_fields.join(',') : media_fields
      end

      params['poll.fields'] = poll_fields.is_a?(Array) ? poll_fields.join(',') : poll_fields unless poll_fields.nil?

      params['user.fields'] = user_fields.is_a?(Array) ? user_fields.join(',') : user_fields unless user_fields.nil?

      unless place_fields.nil?
        params['place.fields'] =
          place_fields.is_a?(Array) ? place_fields.join(',') : place_fields
      end

      @client.request(method: :get, path: path, params: params)
    end

    # Evaluate a Community Note

    # Endpoint to evaluate a community note.

    # @param body [Hash] Request body

    # @return [Hash] API response
    def evaluate(body: nil)
      # Build the request path
      path = '/2/evaluate_note'

      # Build query parameters
      params = {}

      # Prepare request body
      if body.is_a?(Hash)
        body
      else
        (body.nil? ? nil : body)
      end

      @client.request(method: :post, path: path, params: params)
    end

    # Create a Community Note

    # Creates a community note endpoint for LLM use case.

    # @param body [Hash] Request body

    # @return [Hash] API response
    def create(body: nil)
      # Build the request path
      path = '/2/notes'

      # Build query parameters
      params = {}

      # Prepare request body
      if body.is_a?(Hash)
        body
      else
        (body.nil? ? nil : body)
      end

      @client.request(method: :post, path: path, params: params)
    end

    # Search for Community Notes Written

    # Returns all the community notes written by the user.

    # @param test_mode [Boolean] If true, return the notes the caller wrote for the test. If false, return the notes the caller wrote on the product. (required)

    # @param pagination_token [String, nil] Pagination token to get next set of posts eligible for notes.

    # @param max_results [Integer, nil] Max results to return.

    # @param note_fields [Array, nil] A comma separated list of Note fields to display.

    # @return [Hash] API response
    def search_written(test_mode:, pagination_token: nil, max_results: nil, note_fields: nil)
      # Build the request path
      path = '/2/notes/search/notes_written'

      # Build query parameters
      params = {}

      params['test_mode'] = test_mode unless test_mode.nil?

      params['pagination_token'] = pagination_token unless pagination_token.nil?

      params['max_results'] = max_results unless max_results.nil?

      params['note.fields'] = note_fields.is_a?(Array) ? note_fields.join(',') : note_fields unless note_fields.nil?

      @client.request(method: :get, path: path, params: params)
    end

    # Delete a Community Note

    # Deletes a community note.

    # @param id [Object] The community note id to delete. (required)

    # @return [Hash] API response
    def delete(id:)
      # Build the request path
      path = '/2/notes/{id}'

      path = path.gsub('{id}', id.to_s)

      # Build query parameters
      params = {}

      @client.request(method: :delete, path: path, params: params)
    end
  end
end
